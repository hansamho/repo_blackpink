<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotelstay.infra.kakaoLogin.KakaoLoginDao">

	<resultMap id="resultMapObj" type="com.hotelstay.infra.kakaoLogin.KakaoLoginDto"></resultMap>
	
	<select id="kakaoSelectOne" resultMap="resultMapObj">
		SELECT 
		memberName,
		email
		FROM member
		WHERE 1=1 
		AND memberSeq=#{memberSeq}
	</select>
	
	
	<insert id="kakaoLogin">
		INSERT INTO member(
			email
		)VALUES(
			#{email}
		)
	</insert>
	
</mapper>