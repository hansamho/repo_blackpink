<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotelstay.infra.booking.BookingDao">

	<resultMap id="resultMapObj"
		type="com.hotelstay.infra.booking.BookingDto"></resultMap>

	<select id="selectMy" resultMap="resultMapObj">
		SELECT
		a.bookingSeq
		,a.bkCheckin
		,a.bkCheckout
		,a.bkAdult
		,a.bkChild
		,a.bkTotalPrice
		,a.member_seq as memberSeqF
		,b.memberName
		,b.memberID
		,c.roomName
		,c.roomTypeCD
		,c.roomPrice
		,c.roomRegDate
		FROM hotelbooking a
		LEFT JOIN member b
		ON a.bookingSeq = b.memberSeq
		LEFT
		join roomdetail c
		ON a.bookingSeq = c.roomSeq
		WHERE a.member_seq = #{memberSeqF}
	</select>

	<select id="selectOne" resultMap="resultMapObj">
		SELECT
		a.member_seq as memberSeqF
		,b.memberName
		,b.memberPhone
		,b.birthday
		,b.codeNumber
		,b.addrDetail
		,b.roadName
		FROM hotelbooking a
		LEFT JOIN member b
		ON a.bookingSeq = b.memberSeq
		LEFT
		join roomdetail c
		ON a.bookingSeq = c.roomSeq
		WHERE a.member_seq = #{memberSeqF}
	</select>

	<select id="selectWish" resultMap="resultMapObj">
		SELECT
		a.member_seq as memberSeqF
		,c.roomName
		,c.roomPrice
		,c.roomTotalRating
		FROM wishlist a
		LEFT JOIN member b
		on a.wishSeq = b.memberSeq
		LEFT JOIN roomDetail c
		on a.wishSeq = c.roomSeq
		WHERE member_seq = #{memberSeqF};

	</select>

	<update id="memberUpdate">
		UPDATE member
		set
		memberName = #{memberName}
		,birthday = #{birthday}
		,memberPhone = #{memberPhone}
		,codeNumber = #{codeNumber}
		,roadName = #{roadName}
		,addrDetail = #{addrDetail}
		WHERE memberSeq = #{memberSeqF}
	</update>
	
	<select id="bookingSelectOne">
		SELECT
		a.memberSeq
		,a.memberName
        ,a.birthday
        ,a.memberPhone
        ,b.bkCheckin
        ,b.bkCheckout
        ,b.bkAdult
        ,b.bkChild
        ,b.bkTotalPrice
        ,b.payType
        ,b.cardNumber
        ,b.cardDate
        ,b.cardYear
        ,b.cardCcv
        ,c.roomName
        ,c.roomTypeCD
        ,c.roomPrice
		FROM member a
		LEFT JOIN hotelbooking b on b.bookingSeq = a.memberSeq
		LEFT JOIN roomdetail c on b.bookingSeq = c.roomSeq
		WHERE 1=1
		AND a.memberSeq = #{memberSeq};
	</select>
	
	
	
</mapper>